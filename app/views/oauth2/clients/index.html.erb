<h1>Listing clients</h1>

<table>
  <tr>
	<th>Name</th>
	<th>Website</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clients.each do |client| %>
  <tr>
	<td><%=client.name%></td>
	<td><%=client.website%></td>
    <td><%= link_to 'Show', oauth2_client_path(client) %></td>
    <td><%= link_to 'Edit', edit_oauth2_client_path(client) %></td>
    <td><%= link_to 'Destroy', oauth2_client_path(client) , :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Client', new_oauth2_client_path %>
